logs/
*.log

# Artefatos de cobertura: gerados a cada execução do gate, e o diff deles é
# ruído puro — 318 linhas mudaram no último commit sem significar nada.
agent/cover.out

# Credenciais vivem no volume durável do droplet, nunca no repositório.
**/anthropic.env

# Estado do backend de observabilidade que roda no Mac: banco do Grafana,
# blocos do Tempo, indice do VictoriaLogs. Apagar o diretorio e o "comecar
# limpo" completo.
data/

# Objeto BPF compilado ao lado do fonte: sobra de compilação manual.
# O que vale é probe/cmd/agent-probe/*.bpf.o, que o //go:embed consome.
probe/internal/bpf/*.o
